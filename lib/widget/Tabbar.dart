import 'package:flutter/material.dart';
class TabbarOlustur extends StatelessWidget {
  const TabbarOlustur({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
